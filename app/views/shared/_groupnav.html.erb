<nav class="navbar" id="groupnav" role="navigation" aria-label="main navigation">
    <div class="field is-grouped">
    <div id="navbar" class="navbar-menu">
      <div class="field is-grouped">
        <div class="navbar-end">
            <div class="field is-grouped">
            <% if current_user.id == group.admin_id %>
              <div class="navbar-item"> 
                <span class="control"><%= link_to 'Edit', edit_group_path(group) %></span>
              </div>
              <div class="navbar-item"> 
                <span class="control"><%= link_to 'Destroy', group, method: :delete, data: { confirm: 'Are you sure?' } %></span>
              </div>
              <% end %>
            </div>
        </div>
      </div>
    </div>
  </div>
</nav>